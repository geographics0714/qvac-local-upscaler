node_modules/
.claude/
*.upscaled.png
comparison.html
.qvac/
qvac.config.json
npm-debug.log*
.DS_Store

# Keep arbitrary local test photos (e.g. your own snapshots) out of the
# public repo — only the generated smoke-test image and its own output are
# tracked, since those are the only samples/ assets we actually own.
samples/*
!samples/test-gradient.png
!samples/test-gradient.upscaled.png
!samples/comparison.html
!samples/README.md
